import React from "react";
import ReactDOM from "react-dom/client";
import CrushBot from "./CrushBot.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CrushBot />
  </React.StrictMode>
);
